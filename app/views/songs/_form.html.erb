<%= render('layouts/errors', :object => @song) %>

<%= form_for [@user, @song] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist %>
  <%= f.text_field :artist %>
  <p><%= f.submit %></p>
<% end %>
